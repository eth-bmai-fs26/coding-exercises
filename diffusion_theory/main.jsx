import React from 'react';
import ReactDOM from 'react-dom/client';
import GaussianVisualizer from './GaussianVisualizer.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GaussianVisualizer />
  </React.StrictMode>
);
